import { PicInitialsPipe } from './pic-initials.pipe';

describe('PicInitialsPipe', () => {
  it('create an instance', () => {
    const pipe = new PicInitialsPipe();
    expect(pipe).toBeTruthy();
  });
});
