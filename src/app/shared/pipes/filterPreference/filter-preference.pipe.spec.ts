import { FilterPreferencePipe } from './filter-preference.pipe';

describe('FilterPreferencePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPreferencePipe();
    expect(pipe).toBeTruthy();
  });
});
